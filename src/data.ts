import type { AppSettings, Product } from "./types";

export const DEFAULT_SETTINGS: AppSettings = {
  establishmentName: "Adega Tá no Grale",
  defaultServiceFee: false,
};

export const DEFAULT_PRODUCTS: Product[] = [];
